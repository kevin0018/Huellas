# Same-origin HTTP and WebSocket connections through Vite's development proxy.
VITE_API_URL=/api
VITE_SOCKET_URL=/
# Vite's default API target is http://localhost:3000.
# To override it, set API_PROXY_TARGET in the shell that starts pnpm dev.
